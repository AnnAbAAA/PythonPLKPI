#Знайти всі тризначні числа, які одночасно є квадратами і закінчуються на цифру 1.
lst = []
lst2 = []
for i in range(100,999):
    for n in range (1, i):
        if n*n == i:
            lst.append(i)
for i in range(len(lst)):
    if lst[i] % 10 == 1:
        lst2.append(lst[i])

print(lst2)