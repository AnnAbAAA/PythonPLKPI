#8. Дано одновимірний масив із 10 чисел. Написати програму, яка:
#- Знаходить другий за величиною елемент масиву.
#- Перевіряє, чи є всі елементи масиву унікальними.

import random

lst=[]

for i in range(10):
    lst.append(random.randint(1,100))

print(lst)

lst1 = lst
lst1.remove(max(lst1))
second_max = max(lst1)
print(second_max)

t = True
for i in range(len(lst)):
    for j in range(i + 1, len(lst)):
        if lst[i] == lst[j]:
            t = False
            break
print(t)
